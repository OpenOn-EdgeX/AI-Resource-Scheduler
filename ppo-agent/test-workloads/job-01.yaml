apiVersion: batch/v1
kind: Job
metadata:
  name: workload-01
  namespace: scheduler-0324
  labels:
    group: low-load
spec:
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: worker
          image: curlimages/curl:latest
          env:
            - name: SCHEDULER
              value: "http://ppo-agent.edge-system.svc.cluster.local:8080"
            - name: POD_NAME
              value: "workload-01"
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "[workload-01] /allocate 요청"
              curl -s -X POST $SCHEDULER/allocate \
                -H "Content-Type: application/json" \
                -d '{"pod_name":"workload-01","namespace":"scheduler-0324","requested_cores":30,"requested_memory":1024}'
              echo ""
              echo "[workload-01] 작업 시뮬레이션 (3s)"
              sleep 3
              echo "[workload-01] /feedback 전송"
              curl -s -X POST $SCHEDULER/feedback \
                -H "Content-Type: application/json" \
                -d '{"pod_name":"workload-01","allocated_cores":30,"allocated_memory":1024,"actual_performance":0.90,"completed":true}'
              echo ""
