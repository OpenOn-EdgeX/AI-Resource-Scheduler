apiVersion: batch/v1
kind: Job
metadata:
  name: workload-30
  namespace: scheduler-0324
  labels:
    group: recovery
spec:
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: worker
          image: curlimages/curl:latest
          env:
            - name: SCHEDULER
              value: "http://ppo-agent.edge-system.svc.cluster.local:8080"
            - name: POD_NAME
              value: "workload-30"
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "[workload-30] /allocate 요청"
              curl -s -X POST $SCHEDULER/allocate \
                -H "Content-Type: application/json" \
                -d '{"pod_name":"workload-30","namespace":"scheduler-0324","requested_cores":40,"requested_memory":2048}'
              echo ""
              echo "[workload-30] 작업 시뮬레이션 (3s)"
              sleep 3
              echo "[workload-30] /feedback 전송"
              curl -s -X POST $SCHEDULER/feedback \
                -H "Content-Type: application/json" \
                -d '{"pod_name":"workload-30","allocated_cores":40,"allocated_memory":2048,"actual_performance":0.85,"completed":true}'
              echo ""
